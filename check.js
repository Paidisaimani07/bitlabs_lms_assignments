const fs = require('fs'); const data = fs.readFileSync('public/sql/Data Manipulation Language (DML)/html5/data/js/data.js', 'utf8'); const match = data.match(/window\.globalProvideData\('data', '(\{.*?\})'\);/); if(match) { try { let jsonStr = match[1]; jsonStr = jsonStr.replace(/\\\\'/g, "'"); const json = JSON.parse(jsonStr); console.log(Object.keys(json)); if(json.project && json.project.scenes) { let t = 0; json.project.scenes.forEach(s => t += s.slides.length); console.log('Total Slides:', t); } } catch(e) { console.log(e); } } else { console.log('not found'); }
